export default {
    $calc: {
        add(first, second) {
            return first + second;
        },
        subsctract(first, second) {
            return first - second;
        },
        multiply(first, second) {
            return first * second;
        },
        divide(first, second) {
            return first / second;
        }
    }
}